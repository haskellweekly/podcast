version: v1.0
name: Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Build
    task:
      jobs:
        - name: Job
          commands:
            - checkout
            - docker run --rm --volume "$(pwd):/root/podcast" --workdir /root/podcast alpine:3.9.2 sh -c 'apk add --no-cache cabal ghc musl-dev wget && cabal new-update && cabal new-build && cabal new-exec podcast'
            - cd output
            - git init
            - git add .
            - git commit --author 'Semaphore CI <info@haskellweekly.news>' --message "Automatic deploy of $SEMAPHORE_GIT_SHA"
            - git push --force --set-upstream origin gh-pages
